/**
 * Configuración centralizada del frontend.
 * La URL base del API Gateway se define aquí — nunca hardcodeada en los servicios.
 */
export const API_BASE_URL = 'https://rfs00jdfqb.execute-api.us-east-1.amazonaws.com/Prod';
